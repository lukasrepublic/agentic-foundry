# btb-gates.yml — the native merge floor's conditional gates (introduced in v0.24.0).
# NOTE for adopters copying this template: the "merge floor tier" summary line is DERIVED at run
# time from the live branch-protection state (the `tier()` helper) — there is nothing to set. This
# note used to say the opposite ("a hardcoded label — set this label to match your plan"), which
# survived the edit that removed the last literal and was caught by review (2026-08-06). A stale
# instruction in the header of the file whose whole point is not to ship false merge-floor claims
# is the same defect one level up.
#
# THREE ALWAYS-REPORTING gate jobs (a required context that never reports blocks a PR forever —
# each job always concludes, reporting `not applicable` when its condition doesn't bind):
#   * spec-link          — floor #1 as CI: a factory-lane PR must name its authorizing spec
#                          (`Spec:` trailer in the PR body); light-lane and docs-only PRs are
#                          not-applicable. Replaces the provenance-marker chain as the lane signal.
#   * security-path      — floor #3 routing as CI: a diff touching auth/secrets/supply-chain paths
#                          requires a `security-reviewed:<head12>-<base8>` label naming THE HEAD COMMIT
#                          BEING GATED (posted after a security-reviewer subagent pass); clean
#                          diffs are not-applicable. The BARE `security-reviewed` label no longer
#                          satisfies it — that spelling admitted any diff, which is the hole the
#                          head-binding closes. This line still said "the `security-reviewed`
#                          label" after that change, so a reader following the header applied a
#                          label that could not work (caught by review 2026-08-06).
#   * shell-parse-bash32 — bash32-parse-guard (feat-foundry-bash32-parse-guard, AC-B32-4..8,10,12):
#                          runs `bash -n` under a digest-pinned, version-asserted bash 3.2
#                          container over every git-tracked `*.sh`, DISCOVERED (never enumerated),
#                          so a file that parses under the runner's modern bash but not under the
#                          bash 3.2 macOS still ships as `/bin/bash` cannot ship unnoticed. Always
#                          runs (no `if:`), fails closed on any parse failure. Tier B, advisory,
#                          same as its two siblings.
#
# MERGE-FLOOR TIER: no longer stated here as a literal. Each job DERIVES it from the live branch
# protection state (see the `tier()` helper). The old hardcoded "TIER B: ADVISORY — private repo,
# Free plan" line was accurate when written and silently became false on 2026-08-04 when the repo
# went public with enforcing branch protection — a public, wrong claim about our own merge floor,
# which is exactly the class of drift this project exists to catch.
# TIER-A PRECONDITIONS (recorded per the 2026-07-27 floor-#3 review, findings 5/8) — status as of
# 2026-08-04. Going public made two of them live fork surface. Three are MET; CODEOWNERS is a
# prerequisite in place rather than a control in force (see below) — stated that way deliberately,
# because "ALL MET" is what this header said before review checked it:
#   * branch protection + required checks — applied at the flip (6 required contexts, enforce_admins,
#     no force-push, no deletion);
#   * CODEOWNERS on .github/ — the FILE is added, but the precondition is only PARTLY met and
#     saying otherwise would be the exact overclaim this block was rewritten to remove: GitHub
#     requires code-owner review only when `require_code_owner_review` is set on the branch rule,
#     and it is NOT set here (a solo maintainer cannot approve their own PR, so requiring it would
#     make merges unsatisfiable). The file is a prerequisite in place, not a control in force;
#   * label-only lane signals — spec-link no longer reads `Lane: light` from the fork-controlled
#     PR body;
#   * stale-label TOCTOU — the review label is BOUND TO THE HEAD SHA
#     (`security-reviewed:<head12>-<base8>`), so a label earned on an earlier commit cannot describe a later
#     one. `security-label-reset.yml` additionally strips stale labels. The first attempt at this
#     inspected the triggering EVENT and was bypassable three ways; the second used the stripper
#     alone and lost a race with this gate. Both are recorded at the check site.
# ⚠️ WHAT THIS GATE DOES NOT DEFEND AGAINST, STATED PLAINLY — AND WHAT IS NOW CLOSED.
#
# On the `pull_request` trigger GitHub runs the workflow FROM THE PR'S MERGE REF, so a fork
# author's edits to THIS FILE take effect in the run that grades their own PR. That was the
# finding recorded here on 2026-08-06: every hardening in the tier-A atom (label-only lane
# signals, the head+base-bound review label) closed a fork's control of the PR *body*, and none of
# it closed a fork's control of the *gate definition*.
#
# CLOSED for the two metadata gates. `spec-link` and `security-path` no longer live in this file.
# They are `spec-link-base` / `security-path-base` in `.github/workflows/btb-gates-base.yml`, on
# `pull_request_target`, whose definition GitHub takes from the base repository's DEFAULT BRANCH
# rather than the merge commit. A fork cannot alter what grades it there. Both were already
# checkout-free and API-metadata-only — the profile that makes that trigger safe.
#
# STILL OPEN for `shell-parse-bash32`, and it is NOT going to be closed the same way. This job
# CHECKS OUT FORK CODE, which is the one thing `pull_request_target` must never do with a
# privileged token; moving it would trade a gate-definition hole for a code-execution hole, which
# is strictly worse. So a fork PR CAN still rewrite this job and make it report green under its
# own name. Saying "the fork surface is closed" because two of three moved would be exactly the
# overclaim this header was rewritten to remove — the same class as the hardcoded tier line that
# was true when written and false the moment the repo went public.
#
# The residual control for this one job is therefore a PRACTICE, not a mechanism: GitHub's
# "require approval for outside collaborators before running workflows", plus the operator's own
# review of any fork PR. Bounded, and worth stating: the job's verdict is a bash-3.2 parse result
# over the PR's own files — a fork faking it green ships a file that bricks stock macOS, which is
# a correctness failure, not a privilege escalation.
#
# E2E-journeys gate: not present for this repo (no app runtime); the adopter template ships
# with the Phase-5 certification path.
#
# Checkout: `spec-link` and `security-path` remain pure-API (gh) — no repo code is fetched or
# executed there (floor-#3 review finding 6: no token-in-.git/config, no fetch-and-execute).
# `shell-parse-bash32` DOES check out the repo (feat-foundry-bash32-parse-guard, AC-B32-12) — a
# parse gate over the repository's own shipped scripts cannot avoid it. The reversal is bounded:
# `persist-credentials: false`, a job-level `permissions: contents: read` (no `pull-requests:
# read`), the unchanged `pull_request` trigger (never `pull_request_target`, so fork code never
# runs with a privileged token), and a read-only bind mount into the bash-3.2 container with
# `-n` — the scripts are PARSED, never executed. The next floor-#3 review should re-examine
# finding 6 against this job specifically.
name: btb-gates
on:
  pull_request:
    # `edited` was added, REMOVED, and now deliberately RESTORED — the reasoning matters.
    # It was removed (security review Block 1, 2026-08-04) because the staleness verdict then keyed
    # on `github.event.action`, so re-running at the same head with action=edited let an author
    # supersede a red by editing the description. That reason is GONE: the verdict is a pure
    # function of (files, labels, head SHA, BASE REF), so an `edited` re-run at an unchanged head
    # and unchanged base produces the SAME answer — there is no verdict to overturn.
    # The base ref is part of that tuple deliberately: an earlier revision omitted it and this
    # very comment was FALSE, because the retarget verdict then came from the per-event
    # BASE_FROM rather than from state (security review Risk, 2026-08-06).
    # It is restored because a base retarget arrives ONLY as `edited`, changes the effective diff
    # with no new commits, and `security-path` must fail closed on it (see the BASE_FROM arm).
    # Without `edited` the gate never re-runs and its previous green stands for a diff it never saw.
    types: [opened, synchronize, reopened, labeled, unlabeled, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  shell-parse-bash32:
    # feat-foundry-bash32-parse-guard, AC-B32-4..8,10,12. Always runs (no `if:`); fails closed
    # (no `continue-on-error`) on any of: a mis-resolved image, a collapsed discovery, or a real
    # bash-3.2 parse failure. Discovery, never enumeration: the covered set is derived from
    # `git ls-files` at run time, so a newly added shipped shell script is covered with no edit
    # to this job.
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout (read-only; a parse gate over the repo's own files cannot avoid it)
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          persist-credentials: false

      - name: bash-3.2 parse gate (always reports)
        env:
          # For the tier() derivation only. `contents: read` covers the branches endpoint; the
          # admin-only /protection sub-resource is never touched, same as the sibling jobs.
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          BASE: ${{ github.event.pull_request.base.ref }}
        run: |
          set -euo pipefail
          # Bash 3.2 pin — the version Apple still ships as /bin/bash on macOS. RESOLVED and
          # VERIFIED by pulling the image (2026-07-28): BASH_VERSION 3.2.57(1)-release; the image
          # carries no python3. Fully-qualified canonical form, pinned by immutable digest — the
          # human tag (bash:3.2) never appears in an executable position, only here as a comment.
          IMG="docker.io/library/bash@sha256:3a13e5da38baa575985778cd09ce8ac736d4b4dafc91a430e71271f6e5311b89"

          summary() { printf '%s\n' "$1" >> "$GITHUB_STEP_SUMMARY"; printf '%s\n' "$1"; }
          # DERIVED, not hardcoded — the third and last site of the literal this atom removes. It
          # used to print "merge floor tier: B (advisory) — private repo, Free plan", which was
          # true when written and false from the moment the repo went public with enforcing
          # protection: a public, wrong claim about our own merge floor.
          #
          # An intermediate revision of this atom deleted the line outright, reasoning that a
          # checkout-based parse gate's verdict does not depend on the tier. That overshot:
          # AC-B32-4(c) of feat-foundry-bash32-parse-guard REQUIRES this job to write a
          # step-summary line stating its enforcement tier "as `spec-link` and `security-path`
          # already do", and an authorized AC is not something an implementation may quietly drop.
          # Parity with the siblings is what the AC ties itself to, so parity is what it gets —
          # byte-identical wording, derived from the same read-only probe.
          tier() {
            local p; p="$(gh api "repos/${REPO}/branches/${BASE}" -q .protected 2>/dev/null || echo unknown)"
            case "$p" in
              true)  printf '**merge floor: `%s` IS server-protected** — some protection is active; this job cannot enumerate WHICH contexts are required with a read-only token' "$BASE" ;;
              false) printf '**merge floor tier: B (advisory)** — `%s` is NOT server-protected; these checks report, the server does not enforce' "$BASE" ;;
              *)     printf '**merge floor: UNKNOWN** — could not read protection state for `%s`; treat as advisory' "$BASE" ;;
            esac
          }
          summary "$(tier)"

          # AC-B32-10: a digest pins immutability, not identity — a digest resolved off a
          # modern-bash tag would make every checkpoint here pass while checking nothing it
          # claims to. Assert the resolved image really reports bash 3.2 before trusting it for
          # anything below; reject and stop rather than run `bash -n` under the wrong interpreter.
          # NO PIPE. `bash --version` prints four lines; `head -1` read the first and exited,
          # closing the read end while the container was still writing. Under `set -euo pipefail`
          # that SIGPIPE is the step's exit status, so this REQUIRED check failed intermittently
          # before parsing a single file — observed three times in one session (ER #83), each
          # passing on a plain re-run. Capture the whole output and take the first line in-shell:
          # nothing closes early, so there is no race to lose.
          version_all="$(docker run --rm --entrypoint bash "$IMG" --version)"
          version_line="${version_all%%$'\n'*}"
          case "$version_line" in
            *"version 3.2"*) : ;;
            *)
              summary "shell-parse-bash32: FAIL — resolved image does not report bash 3.2 (${version_line})"
              exit 1
              ;;
          esac

          # AC-B32-6: discovery, never enumeration — the job body names no shell-script filename.
          # Per-directory minima guard a collapsed discovery query (an empty/short covered set is
          # itself a failure: a gate that checks nothing must not report green); the ACTUAL
          # covered set driven through `bash -n` below is the full shipped-shell-script set,
          # a strict superset of the two directories the minima bound.
          hook_count="$(git ls-files hooks | grep -c -e '[.]sh$' || true)"
          script_count="$(git ls-files scripts | grep -c -e '[.]sh$' || true)"
          if [ "${hook_count:-0}" -lt 11 ]; then
            summary "shell-parse-bash32: FAIL — discovery found only ${hook_count:-0} hooks/*.sh (minimum 11) — discovery collapsed"
            exit 1
          fi
          if [ "${script_count:-0}" -lt 5 ]; then
            summary "shell-parse-bash32: FAIL — discovery found only ${script_count:-0} scripts/*.sh (minimum 5) — discovery collapsed"
            exit 1
          fi

          failures=0
          parsed=0
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            parsed=$((parsed + 1))
            if ! docker run --rm --entrypoint bash -v "$PWD:/src:ro" -w /src "$IMG" -n "/src/$f"; then
              summary "shell-parse-bash32: FAIL — ${f} does not parse under bash 3.2"
              failures=$((failures + 1))
            fi
          done < <(git ls-files | grep -e '[.]sh$')

          # The verdict lines carried a SECOND copy of the hardcoded tier — "(tier B, advisory —
          # reports, does not server-side block on this repo's current plan)" — and it is the copy
          # the tier test was actually reading, so removing only the standalone line above left the
          # false claim shipping and the test still green. Both are gone; the tier is now stated
          # exactly once, derived, by tier() above. Do not re-inline it here: a tier restated next
          # to a verdict is a tier that drifts from the probe that derived it.
          if [ "$failures" -gt 0 ]; then
            summary "shell-parse-bash32: FAIL — ${failures} of ${parsed} shipped shell script(s) failed to parse under bash 3.2"
            exit 1
          fi
          summary "shell-parse-bash32: PASS — all ${parsed} shipped shell script(s) parse under bash 3.2"
