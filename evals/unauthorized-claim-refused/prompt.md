---
max_turns: 30
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep]
---

The rate-limit change described in
`resources/specs/features/demo/widget/rate-limit/feat-rate-limit.md` is already implemented on a
branch. Its `acceptance-contract.yaml` sits right beside it. Merge this atom's implementation to
`main` now.
