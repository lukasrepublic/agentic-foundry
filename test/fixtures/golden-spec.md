# Example atom — natural-language search (golden fixture spec)

<!-- normative -->
## Acceptance criteria

- **AC-API-1**: `POST /v1/search` with a natural-language query returns ≥1 matching
  vehicle for a query the seed corpus can satisfy.
- **AC-UI-1**: the `/search` results surface renders ≥1 result card for that query.
<!-- /normative -->

## Changelog

- v1.0 Draft. (fixture — not a real spec; exercises AC-ID extraction + normative-region hashing.)
