thresholds:
  silent_yield:
    max: 1.0
  granted_verb_denial:
    max: 1.0
  guard_false_positive_sample:
    sampled: true
# directive_reply intentionally omitted (round-2 review AC-CBR-2 fixture): certify() must report
# it as "directive_reply no threshold configured" in the verdict line, never silently pass by
# omission.
